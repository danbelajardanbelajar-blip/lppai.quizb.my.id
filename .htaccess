# Prevent directory listing and ensure the root index is used.
Options -Indexes
DirectoryIndex index.php

# Root requests are redirected by index.php, not by rewrite rules.
# This avoids redirect loops on some Apache configurations.
